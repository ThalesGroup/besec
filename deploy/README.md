This Terraform config can be used to deploy an instance of BeSec to a GCP project.

Set the `gcp-project` and `gcp-region` variables to specify where to deploy it,
and populate the `backend` entry in `backend.tf`.
